<nav class="navMain">
      <div onclick="toggleNav()"  class="resize">
      <div class="menu" onclick="menuFunction(this)">
          <div class="bar1"></div>
          <div class="bar2"></div>
          <div class="bar3"></div>
      </div>
    </div>
</nav>
<div class="logo">
  <a href="/"></a>
</div>
{{> nav/shoppingLogo}}


<nav class="sideNav">
  <ul>
    <!-- The different page links -->

    {{> nav/home}}<!-- End of auth home -->

    {{#if (noEqual user)}}
      {{> nav/account}}
      {{> nav/about}}
      <li>
        <a href="/logout">Logout</a>
      </li>
      
    {{else}}
      {{> nav/register}}
      {{> nav/login}}
      {{> nav/about}}
      {{> nav/cart}}
    {{/if}}
  </ul>
</nav>